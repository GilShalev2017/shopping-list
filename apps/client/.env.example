# Catalog service (.NET 10 + EF Core + SQL Server) — screen 1 data
VITE_CATALOG_API_URL=http://localhost:5080

# Orders service (NestJS + Elasticsearch / MongoDB) — screen 2 persistence
VITE_ORDERS_API_URL=http://localhost:3000

# Initial UI language before the user picks one: he | en
VITE_DEFAULT_LOCALE=he
